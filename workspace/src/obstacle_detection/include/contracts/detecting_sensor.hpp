#ifndef DETECTING_SENSOR_HPP
#define DETECTING_SENSOR_HPP

namespace magellan
{
    namespace obstacle_detection
    {
        enum DetectingSensor
        {
            DEPTH_CAMERA = 0,
            RGB_CAMERA,
            LIDAR
        };
    }
}

#endif
